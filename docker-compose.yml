version: "3.9"
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: budgetops-fe:latest
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped


